{
  "name": "color-flip",
  "version": "0.0.1",
  "description": "Convert colors between RGB, HEX, and HSL formats",
  "keywords": [
    "color",
    "convert",
    "conversion",
    "rgb",
    "hex",
    "hsl"
  ],
  "homepage": "https://github.com/textencoder/color-flip",
  "bugs": {
    "url": "https://github.com/textencoder/color-flip/issues"
  },
  "author": "textencoder",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/textencoder/color-flip.git"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "build": "tsc",
    "ci": "npm run build && npm run check-format && npm run test",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "test": "vitest run",
    "dev": "vitest",
    "local-release": "changeset version && changeset publish",
    "prePublishOnly": "npm run ci"
  }
}
